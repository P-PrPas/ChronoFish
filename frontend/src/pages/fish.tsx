import { type FormEvent, useCallback, useEffect, useMemo, useState } from 'react'
import { type ApiItem, get } from '../api/client'
import { putQueue, type QueuedWrite } from '../offline'
import { type AppText } from '../types'
import { Empty, ErrorMessage, ReportTable } from '../components'

type FishMode = 'rollcall' | 'registry'
type FishOutcome = 'ALIVE' | 'DEAD' | 'FROZEN' | 'DISCARDED'
const bangkokDate = () => { const parts = new Intl.DateTimeFormat('en', { timeZone: 'Asia/Bangkok', year: 'numeric', month: '2-digit', day: '2-digit' }).formatToParts(new Date()); const value = Object.fromEntries(parts.map((part) => [part.type, part.value])); return `${value.year}-${value.month}-${value.day}` }

export function Fish({ t, onPendingChange }: { t: AppText; onPendingChange: (count: number) => void }) {
  const [items, setItems] = useState<ApiItem[]>([]); const [registry, setRegistry] = useState<ApiItem[]>([]); const [selectedFish, setSelectedFish] = useState(''); const [mode, setMode] = useState<FishMode>('rollcall'); const [date, setDate] = useState(bangkokDate()); const [outcomes, setOutcomes] = useState<Record<string, FishOutcome>>({}); const [search, setSearch] = useState(''); const [filters, setFilters] = useState({ siteId: '', boxId: '', status: '', strain: '', treatmentGroupId: '', dobFrom: '', dobTo: '' }); const [masters, setMasters] = useState<Record<string, ApiItem[]>>({ sites: [], 'fish-boxes': [], 'treatment-groups': [] }); const [showCreate, setShowCreate] = useState(false); const [error, setError] = useState('')
  const loadRollCall = useCallback(() => { void get(`/fish/roll-call?date=${date}`).then((data) => { setItems(data.items ?? []); onPendingChange(0) }).catch((e: Error) => setError(e.message)) }, [date, onPendingChange])
  const loadRegistry = useCallback(() => { void get('/fish?includeInactive=true').then((data) => setRegistry(data.items ?? [])).catch((e: Error) => setError(e.message)) }, [])
  useEffect(() => { void Promise.all(['sites', 'fish-boxes', 'treatment-groups'].map((resource) => get(`/${resource}`).then((data) => [resource, data.items ?? []] as [string, ApiItem[]]))).then((result) => setMasters(Object.fromEntries(result))).catch(() => undefined) }, [])
  useEffect(() => { if (mode === 'rollcall') loadRollCall(); else loadRegistry() }, [mode, loadRollCall, loadRegistry])
  useEffect(() => { const refresh = () => { loadRollCall(); loadRegistry() }; const reject = (event: Event) => { const detail = (event as CustomEvent<QueuedWrite>).detail; if (detail.path.startsWith('/observations/fish')) setError(detail.lastError ?? 'Queued fish observation rejected') }; window.addEventListener('chronofish:queue-drained', refresh); window.addEventListener('chronofish:queue-rejected', reject); return () => { window.removeEventListener('chronofish:queue-drained', refresh); window.removeEventListener('chronofish:queue-rejected', reject) } }, [loadRollCall, loadRegistry])
  const record = async (fish: ApiItem, outcome: FishOutcome) => { const id = String(fish.fishId); const previous = outcomes[id]; setOutcomes((current) => ({ ...current, [id]: outcome })); try { await putQueue('/observations/fish', { observations: [{ clientUuid: crypto.randomUUID(), cloneFishId: fish.fishId, observedOn: date, outcome, condition: fish.condition ?? 'NORMAL' }] }); loadRollCall() } catch (e) { setOutcomes((current) => ({ ...current, [id]: previous ?? 'ALIVE' })); setError((e as Error).message) } }
  const markAlive = async () => { await Promise.all(items.filter((item) => !item.alreadyRecorded).map((item) => record(item, 'ALIVE'))) }
  const visibleRegistry = registry.filter((fish) => (!filters.siteId || String(fish.siteId) === filters.siteId) && (!filters.boxId || String(fish.fishBoxId) === filters.boxId) && (!filters.status || String(fish.status) === filters.status) && (!filters.treatmentGroupId || String(fish.treatmentGroupId) === filters.treatmentGroupId) && (!filters.strain || String(fish.strain ?? '').toLowerCase().includes(filters.strain.toLowerCase())) && (!filters.dobFrom || String(fish.dob) >= filters.dobFrom) && (!filters.dobTo || String(fish.dob) <= filters.dobTo) && (!search || `${fish.fishCode ?? ''} ${fish.strain ?? ''}`.toLowerCase().includes(search.toLowerCase())))
  if (selectedFish) return <FishDetail fishId={selectedFish} masters={masters} onBack={() => setSelectedFish('')} />
  return <section><div className="page-heading"><div><p className="eyebrow">STAGE 2 / REGISTRY</p><h1>{t.fish}</h1><p className="muted">Bangkok date: {date}. Record one fish at a time or use the shortcut.</p></div>{mode === 'rollcall' && <div className="button-row"><button className="button button--secondary" onClick={markAlive}>{t.allAlive}</button><button className="button button--primary" onClick={() => setShowCreate(true)}>Register fish</button></div>}{mode === 'registry' && <button className="button button--primary" onClick={() => setShowCreate(true)}>Register fish</button>}</div><div className="tabs" role="tablist" aria-label="Fish workflow"><button role="tab" aria-selected={mode === 'rollcall'} className={mode === 'rollcall' ? 'tab tab--active' : 'tab'} onClick={() => setMode('rollcall')}>Daily roll-call</button><button role="tab" aria-selected={mode === 'registry'} className={mode === 'registry' ? 'tab tab--active' : 'tab'} onClick={() => setMode('registry')}>Fish registry</button></div>{showCreate && <ManualFishForm masters={masters} onSaved={() => { setShowCreate(false); loadRegistry() }} onCancel={() => setShowCreate(false)} />}{error && <ErrorMessage message={error} />}{mode === 'registry' ? <><fieldset className="filter-bar"><legend>Registry filters</legend><label>Search<input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Fish code or strain" /></label><label>Site<select value={filters.siteId} onChange={(event) => setFilters({ ...filters, siteId: event.target.value })}><option value="">All sites</option>{(masters.sites ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.code ?? item.name)}</option>)}</select></label><label>Box<select value={filters.boxId} onChange={(event) => setFilters({ ...filters, boxId: event.target.value })}><option value="">All boxes</option>{(masters['fish-boxes'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.boxCode ?? item.code)}</option>)}</select></label><label>Status<select value={filters.status} onChange={(event) => setFilters({ ...filters, status: event.target.value })}><option value="">All statuses</option><option>ALIVE</option><option>DEAD</option><option>FROZEN</option><option>DISCARDED</option></select></label><label>Strain<input value={filters.strain} onChange={(event) => setFilters({ ...filters, strain: event.target.value })} /></label><label>Treatment<select value={filters.treatmentGroupId} onChange={(event) => setFilters({ ...filters, treatmentGroupId: event.target.value })}><option value="">All treatments</option>{(masters['treatment-groups'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.code ?? item.name)}</option>)}</select></label><label>DOB from<input type="date" value={filters.dobFrom} onChange={(event) => setFilters({ ...filters, dobFrom: event.target.value })} /></label><label>DOB to<input type="date" value={filters.dobTo} onChange={(event) => setFilters({ ...filters, dobTo: event.target.value })} /></label><button className="button button--secondary" type="button" onClick={() => setFilters({ siteId: '', boxId: '', status: '', strain: '', treatmentGroupId: '', dobFrom: '', dobTo: '' })}>Clear</button></fieldset>{visibleRegistry.length === 0 ? <Empty message="No fish match these filters" /> : <div className="list">{visibleRegistry.map((fish) => <button className="list-row" key={String(fish.id)} onClick={() => setSelectedFish(String(fish.id))}><span><strong>{String(fish.fishCode)}</strong><small>{String(fish.strain ?? 'Unknown strain')} · DOB {String(fish.dob ?? '—')} · first abnormality {String(fish.firstAbnormalOn ?? '—')}</small></span><span className="pill">{String(fish.status ?? '—')}</span></button>)}</div>}</> : <><label className="form-card">Roll-call date<input type="date" value={date} onChange={(event) => setDate(event.target.value)} /></label>{items.length === 0 ? <Empty message={t.empty} /> : <div className="list">{items.map((fish) => { const id = String(fish.fishId); const value = outcomes[id] ?? (fish.alreadyRecorded ? String(fish.status ?? 'ALIVE') as FishOutcome : 'ALIVE'); return <div className="list-row" key={id}><button className="fish-row-main" onClick={() => setSelectedFish(id)}><strong>{String(fish.fishCode)}</strong><small>{String(fish.ageDays ?? '—')} days · {String(fish.condition ?? '—')} · first abnormality {String(fish.firstAbnormalOn ?? '—')}</small></button><div className="button-row"><button className={value === 'ALIVE' ? 'button button--primary' : 'button button--secondary'} onClick={() => void record(fish, 'ALIVE')}>Alive</button><button className={value === 'DEAD' ? 'button button--primary' : 'button button--secondary'} onClick={() => void record(fish, 'DEAD')}>Dead</button><button className={value === 'FROZEN' ? 'button button--primary' : 'button button--secondary'} onClick={() => void record(fish, 'FROZEN')}>Frozen</button><button className={value === 'DISCARDED' ? 'button button--primary' : 'button button--secondary'} onClick={() => void record(fish, 'DISCARDED')}>Discarded</button></div></div> })}</div>}</>}</section>
}

function ManualFishForm({ masters, onSaved, onCancel }: { masters: Record<string, ApiItem[]>; onSaved: () => void; onCancel: () => void }) {
  const [form, setForm] = useState({ fishCode: '', dob: bangkokDate(), donorCellLineId: '', siteId: '', fishBoxId: '', condition: 'NORMAL', sex: 'UNKNOWN', remarks: '', overrideReason: '' }); const [donors, setDonors] = useState<ApiItem[]>([]); const [error, setError] = useState('')
  useEffect(() => { void get('/donor-cell-lines').then((data) => setDonors(data.items ?? [])) }, [])
  const set = (key: string, value: string) => setForm((current) => ({ ...current, [key]: value }))
  const submit = async (event: FormEvent) => { event.preventDefault(); try { await putQueue('/fish', { ...form, siteId: form.siteId || null, fishBoxId: form.fishBoxId || null, overrideReason: form.overrideReason || null }); onSaved() } catch (e) { setError((e as Error).message) } }
  return <form className="form-card" onSubmit={submit}><h2>Register clone fish</h2><div className="form-card--inline"><label>Fish code<input required value={form.fishCode} onChange={(e) => set('fishCode', e.target.value)} /></label><label>DOB<input required type="date" value={form.dob} onChange={(e) => set('dob', e.target.value)} /></label><label>Donor cell line<select required value={form.donorCellLineId} onChange={(e) => set('donorCellLineId', e.target.value)}><option value="">Select donor</option>{donors.map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.strain ?? item.id)}</option>)}</select></label></div><div className="form-card--inline"><label>Site<select value={form.siteId} onChange={(e) => set('siteId', e.target.value)}><option value="">No site</option>{(masters.sites ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.code ?? item.name)}</option>)}</select></label><label>Fish box<select value={form.fishBoxId} onChange={(e) => set('fishBoxId', e.target.value)}><option value="">No box</option>{(masters['fish-boxes'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.boxCode ?? item.code)}</option>)}</select></label><label>Sex<select value={form.sex} onChange={(e) => set('sex', e.target.value)}><option>UNKNOWN</option><option>M</option><option>F</option></select></label></div><label>Remarks<input value={form.remarks} onChange={(e) => set('remarks', e.target.value)} /></label><label>Override reason (if older fish)<input value={form.overrideReason} onChange={(e) => set('overrideReason', e.target.value)} /></label>{error && <ErrorMessage message={error} />}<div className="button-row"><button className="button button--primary">Save fish</button><button type="button" className="button button--secondary" onClick={onCancel}>Cancel</button></div></form>
}

function FishDetail({ fishId, masters, onBack }: { fishId: string; masters: Record<string, ApiItem[]>; onBack: () => void }) {
  const [detail, setDetail] = useState<ApiItem | null>(null); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const [editing, setEditing] = useState<ApiItem | null>(null); const [reason, setReason] = useState(''); const [fishEdit, setFishEdit] = useState({ fishCode: '', sex: 'UNKNOWN', fishBoxId: '' }); const [specimen, setSpecimen] = useState({ specimenCode: '', specimenKind: 'CL', specimenType: 'CAUDAL_FIN_CLIP', collectedOn: bangkokDate(), frozenOn: '', storage: '', notes: '', markFinClipped: false })
  const load = useCallback(() => { void get(`/fish/${fishId}`).then((value) => { setDetail(value); setFishEdit({ fishCode: String(value.fishCode ?? ''), sex: String(value.sex ?? 'UNKNOWN'), fishBoxId: String(value.fishBoxId ?? '') }) }).catch((e: Error) => setError(e.message)) }, [fishId])
  useEffect(load, [load])
  const saveFish = async (event: FormEvent) => { event.preventDefault(); setSaving(true); try { await putQueue(`/fish/${fishId}`, { fishCode: fishEdit.fishCode, sex: fishEdit.sex, fishBoxId: fishEdit.fishBoxId || null }, 'application/json', 'PATCH'); setSaving(false); load() } catch (e) { setError((e as Error).message); setSaving(false) } }
  const saveSpecimen = async (event: FormEvent) => { event.preventDefault(); setSaving(true); try { await putQueue(`/fish/${fishId}/specimens`, { ...specimen, frozenOn: specimen.frozenOn || null, storage: specimen.storage || null, markFinClipped: specimen.markFinClipped }); setSpecimen({ ...specimen, specimenCode: '' }); await load() } catch (e) { setError((e as Error).message) } finally { setSaving(false) } }
  const correct = async (event: FormEvent) => { event.preventDefault(); if (!editing || !reason.trim()) return; try { await putQueue(`/observations/fish/${editing.id}`, { correctionReason: reason, observedOn: editing.observedOn, outcome: editing.outcome, condition: editing.condition, notes: editing.notes }, 'application/json', 'PATCH'); setEditing(null); setReason(''); await load() } catch (e) { setError((e as Error).message) } }
  const remove = async (observation: ApiItem) => { const deleteReason = window.prompt('Reason for deleting this observation'); if (!deleteReason?.trim()) return; try { await putQueue(`/observations/fish/${observation.id}?reason=${encodeURIComponent(deleteReason)}`, undefined, 'application/json', 'DELETE'); await load() } catch (e) { setError((e as Error).message) } }
  if (!detail) return <section><button className="back" onClick={onBack}>← Fish registry</button>{error ? <ErrorMessage message={error} /> : <p className="notice">Loading fish record...</p>}</section>
  const observations = (detail.observations as ApiItem[] | undefined) ?? []; const specimens = (detail.specimens as ApiItem[] | undefined) ?? []
  return <section><button className="back" onClick={onBack}>← Fish registry</button><div className="page-heading"><div><p className="eyebrow">FISH DETAIL / TIMELINE</p><h1>{String(detail.fishCode)}</h1><p className="muted">Strain {String(detail.strain ?? '—')} · DOB {String(detail.dob ?? '—')} · {String(detail.status ?? '—')} · condition {String(detail.condition ?? '—')}</p><p className="muted">First abnormality: {String(detail.firstAbnormalOn ?? '—')} · age {String(detail.firstAbnormalAgeDays ?? '—')}</p></div></div>{error && <ErrorMessage message={error} />}<form className="form-card form-card--inline" onSubmit={saveFish}><label>Fish code<input required value={fishEdit.fishCode} onChange={(event) => setFishEdit({ ...fishEdit, fishCode: event.target.value })} /></label><label>Sex<select value={fishEdit.sex} onChange={(event) => setFishEdit({ ...fishEdit, sex: event.target.value })}><option>UNKNOWN</option><option>M</option><option>F</option></select></label><label>Fish box<select value={fishEdit.fishBoxId} onChange={(event) => setFishEdit({ ...fishEdit, fishBoxId: event.target.value })}><option value="">No box</option>{(masters['fish-boxes'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.boxCode ?? item.code)}</option>)}</select></label><button className="button button--primary" disabled={saving}>Save fish</button></form><div className="table-wrap"><table><thead><tr><th>Observed on</th><th>Age</th><th>Outcome</th><th>Condition</th><th>Actions</th></tr></thead><tbody>{observations.length === 0 ? <tr><td colSpan={5}>No observations yet</td></tr> : observations.map((item) => <tr key={String(item.id)}><td>{String(item.observedOn ?? '—')}</td><td>{String(item.ageDays ?? '—')}</td><td>{String(item.outcome ?? '—')}</td><td>{String(item.condition ?? '—')}</td><td><button className="inline-action" onClick={() => { setEditing(item); setReason('') }}>Correct</button><button className="inline-action inline-action--danger" onClick={() => void remove(item)}>Delete</button></td></tr>)}</tbody></table></div>{editing && <form className="form-card form-card--inline" onSubmit={correct}><label>Outcome<select value={String(editing.outcome ?? 'ALIVE')} onChange={(event) => setEditing({ ...editing, outcome: event.target.value })}><option>ALIVE</option><option>DEAD</option><option>FROZEN</option><option>DISCARDED</option></select></label><label>Condition<select value={String(editing.condition ?? 'NORMAL')} onChange={(event) => setEditing({ ...editing, condition: event.target.value as 'NORMAL' | 'ABNORMAL' | 'UNDETERMINED' })}><option>NORMAL</option><option>ABNORMAL</option><option>UNDETERMINED</option></select></label><label>Correction reason<input required value={reason} onChange={(event) => setReason(event.target.value)} /></label><button className="button button--primary" disabled={saving}>Save correction</button><button className="button button--secondary" type="button" onClick={() => setEditing(null)}>Cancel</button></form>}<form className="form-card" onSubmit={saveSpecimen}><h2>Specimen</h2><div className="form-card--inline"><label>Specimen code<input required value={specimen.specimenCode} onChange={(event) => setSpecimen({ ...specimen, specimenCode: event.target.value })} /></label><label>Kind<select value={specimen.specimenKind} onChange={(event) => setSpecimen({ ...specimen, specimenKind: event.target.value })}><option>CL</option><option>RT</option><option>DC</option></select></label><label>Type<select value={specimen.specimenType} onChange={(event) => setSpecimen({ ...specimen, specimenType: event.target.value })}><option>CAUDAL_FIN_CLIP</option><option>WHOLE_EMBRYO</option></select></label></div><div className="form-card--inline"><label>Collected on<input required type="date" value={specimen.collectedOn} onChange={(event) => setSpecimen({ ...specimen, collectedOn: event.target.value })} /></label><label>Frozen on<input type="date" value={specimen.frozenOn} onChange={(event) => setSpecimen({ ...specimen, frozenOn: event.target.value })} /></label><label>Storage<select value={specimen.storage} onChange={(event) => setSpecimen({ ...specimen, storage: event.target.value })}><option value="">Not stored</option><option>-20</option><option>-80</option></select></label></div><label>Notes<input value={specimen.notes} onChange={(event) => setSpecimen({ ...specimen, notes: event.target.value })} /></label><label><input type="checkbox" checked={specimen.markFinClipped} onChange={(event) => setSpecimen({ ...specimen, markFinClipped: event.target.checked })} /> Mark fin clipped</label><button className="button button--primary" disabled={saving}>Add specimen</button></form><ReportTable headers={['Specimen', 'Kind', 'Type', 'Collected', 'Frozen', 'Storage']} rows={specimens.map((item) => [String(item.specimenCode ?? '—'), String(item.specimenKind ?? '—'), String(item.specimenType ?? '—'), String(item.collectedOn ?? '—'), String(item.frozenOn ?? '—'), String(item.storage ?? '—')])} /></section>
}
