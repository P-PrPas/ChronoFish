import { type FormEvent, useCallback, useEffect, useMemo, useState } from 'react'
import { operatorId, type ApiItem, get } from '../api/client'
import { putQueue, type QueuedWrite } from '../offline'
import { type AppText } from '../types'
import { Empty, ErrorMessage } from '../components'
import { dateTimeLocalToRFC3339, rfc3339ToDateTimeLocal } from '../time'

const today = () => new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Bangkok' }).format(new Date())
const dateTimeInput = (value: string) => value ? rfc3339ToDateTimeLocal(value) : rfc3339ToDateTimeLocal(new Date().toISOString())
const wells = Array.from({ length: 96 }, (_, index) => `${String.fromCharCode(65 + Math.floor(index / 12))}${index % 12 + 1}`)

export function Batches({ t }: { t: AppText }) {
  const [items, setItems] = useState<ApiItem[]>([])
  const [selected, setSelected] = useState<ApiItem | null>(null)
  const [showForm, setShowForm] = useState(false)
  const [message, setMessage] = useState('')
  const load = useCallback(() => { void get('/batches').then((data) => setItems(data.items ?? [])).catch((e: Error) => setMessage(e.message)) }, [])
  useEffect(load, [load])
  useEffect(() => { const refresh = () => load(); const reject = (event: Event) => { const detail = (event as CustomEvent<QueuedWrite>).detail; if (detail.path === '/batches') { setItems((current) => current.filter((item) => !item.queued)); setMessage(detail.lastError ?? 'Queued batch rejected') } }; window.addEventListener('chronofish:queue-drained', refresh); window.addEventListener('chronofish:queue-rejected', reject); return () => { window.removeEventListener('chronofish:queue-drained', refresh); window.removeEventListener('chronofish:queue-rejected', reject) } }, [load])
  if (selected) return <BatchDetail batch={selected} onBack={() => setSelected(null)} />
  const addQueued = (batch: ApiItem) => { setItems((current) => [{ ...batch, id: `queued-${Date.now()}`, queued: true }, ...current]); setShowForm(false); setMessage('Saved offline; will sync automatically') }
  return <section><div className="page-heading"><div><p className="eyebrow">EXPERIMENTS</p><h1>{t.batches}</h1><p className="muted">Create batches, pin timing, and add injection lots.</p></div><button className="button button--primary" onClick={() => setShowForm(!showForm)}>+ {t.save}</button></div>{message && <ErrorMessage message={message} />}{showForm && <BatchForm onSaved={() => { setShowForm(false); load() }} onQueued={addQueued} />}{items.length === 0 ? <Empty message={t.empty} /> : <div className="list">{items.map((item) => <button className="list-row" key={String(item.id)} onClick={() => setSelected(item)}><span><strong>{String(item.batchCode)}</strong><small>{String(item.experimentDate)} · profile {String(item.timingProfileVersion ?? item.timingProfileId ?? '')}</small></span><span className="pill">{item.queued ? 'queued' : 'active'}</span></button>)}</div>}</section>
}

function BatchForm({ onSaved, onQueued }: { onSaved: () => void; onQueued: (batch: ApiItem) => void }) {
  const [form, setForm] = useState({ batchCode: '', experimentDate: today(), siteId: '', operatorId: operatorId(), protocolId: '', treatmentGroupId: '', recipientEggLotId: '', csofLotId: '', clutchCode: '', replicateNo: '', incubationTempC: '', notes: '' })
  const [masters, setMasters] = useState<Record<string, ApiItem[]>>({ sites: [], protocols: [], 'treatment-groups': [], 'recipient-egg-lots': [], 'csof-lots': [] })
  const [error, setError] = useState('')
  const protocolDefault = String(masters.protocols?.[0]?.id ?? '')
  useEffect(() => { void Promise.all(['sites', 'protocols', 'treatment-groups', 'recipient-egg-lots', 'csof-lots'].map((resource) => get(`/${resource}`).then((data) => [resource, data.items ?? []] as [string, ApiItem[]]))).then((result) => { const next = Object.fromEntries(result); setMasters(next); setForm((current) => current.protocolId ? current : { ...current, protocolId: String(next.protocols?.[0]?.id ?? '') }) }).catch((e: Error) => setError(e.message)) }, [])
  const set = (key: string, value: string) => setForm((current) => ({ ...current, [key]: value }))
  const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); const payload = { ...form, batchCode: form.batchCode || null, recipientEggLotId: form.recipientEggLotId || null, csofLotId: form.csofLotId || null, clutchCode: form.clutchCode || null, replicateNo: form.replicateNo ? Number(form.replicateNo) : null, incubationTempC: form.incubationTempC ? Number(form.incubationTempC) : null, notes: form.notes || null }; try { const result = await putQueue('/batches', payload) as ApiItem; result.queued ? onQueued(form as unknown as ApiItem) : onSaved() } catch (e) { setError((e as Error).message) } }
  return <form className="form-card" onSubmit={submit}><h2>New batch</h2><p className="muted">Suggested code is editable; leave it blank to let the server generate one.</p><div className="form-card--inline"><label>Batch code<input data-testid="batch-code" value={form.batchCode} placeholder={`${form.experimentDate}_${form.treatmentGroupId || 'treatment'}`} onChange={(e) => set('batchCode', e.target.value)} /></label><label>Experiment date<input required type="date" value={form.experimentDate} onChange={(e) => set('experimentDate', e.target.value)} /></label><label>Operator<select required value={form.operatorId} onChange={(e) => set('operatorId', e.target.value)}><option value={operatorId()}>Current operator</option></select></label></div><div className="form-card--inline"><label>Site<select required value={form.siteId} onChange={(e) => set('siteId', e.target.value)}><option value="">Select site</option>{(masters.sites ?? []).map((site) => <option key={String(site.id)} value={String(site.id)}>{String(site.code)} — {String(site.name)}</option>)}</select></label><label>Protocol<select required value={form.protocolId} onChange={(e) => set('protocolId', e.target.value)}><option value={protocolDefault}>Default SCNT protocol</option></select></label><label>Treatment group<select required value={form.treatmentGroupId} onChange={(e) => set('treatmentGroupId', e.target.value)}><option value="">Select treatment</option>{(masters['treatment-groups'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.code ?? item.name)}</option>)}</select></label></div><div className="form-card--inline"><label>Recipient egg lot<select value={form.recipientEggLotId} onChange={(e) => set('recipientEggLotId', e.target.value)}><option value="">Not linked</option>{(masters['recipient-egg-lots'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.label ?? item.lotCode ?? item.id)}</option>)}</select></label><label>CSOF lot<select value={form.csofLotId} onChange={(e) => set('csofLotId', e.target.value)}><option value="">Not linked</option>{(masters['csof-lots'] ?? []).map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.lotCode ?? item.code ?? item.id)}</option>)}</select></label><label>Clutch code<input value={form.clutchCode} onChange={(e) => set('clutchCode', e.target.value)} /></label></div><div className="form-card--inline"><label>Replicate no.<input type="number" min="1" value={form.replicateNo} onChange={(e) => set('replicateNo', e.target.value)} /></label><label>Incubation °C<input type="number" min="0" max="50" step="0.1" value={form.incubationTempC} onChange={(e) => set('incubationTempC', e.target.value)} /></label><label>Notes<input value={form.notes} onChange={(e) => set('notes', e.target.value)} /></label></div>{error && <ErrorMessage message={error} />}<button className="button button--primary" type="submit">Save batch</button></form>
}

function BatchDetail({ batch, onBack }: { batch: ApiItem; onBack: () => void }) {
  const [detail, setDetail] = useState<ApiItem | null>(null); const [embryos, setEmbryos] = useState<Record<string, ApiItem[]>>({}); const [message, setMessage] = useState(''); const [editing, setEditing] = useState(false); const [editDate, setEditDate] = useState(String(batch.experimentDate ?? '')); const [editCode, setEditCode] = useState(String(batch.batchCode ?? ''))
  const [lot, setLot] = useState({ lotNo: '1', donorCellLineId: '', activatedAt: dateTimeInput(''), enuPowerPct: '', enuPulseUs: '', enuLed: '', enuStartAt: '', enuFinishAt: '', nEggs: '', nActivated: '1', notes: '', wellPositions: '' })
  const [donors, setDonors] = useState<ApiItem[]>([]); const [count, setCount] = useState(1)
  const load = useCallback(() => { void get(`/batches/${batch.id}`).then(async (value) => { setDetail(value); const lots = (value.injectionLots as ApiItem[] | undefined) ?? []; const loaded = await Promise.all(lots.map(async (item: ApiItem) => [String(item.id), (await get(`/injection-lots/${item.id}/embryos?aliveOnly=false`)).items ?? []] as [string, ApiItem[]])); setEmbryos(Object.fromEntries(loaded)) }).catch((e: Error) => setMessage(e.message)) }, [batch.id])
  useEffect(load, [load]); useEffect(() => { void get('/donor-cell-lines').then((data) => setDonors(data.items ?? [])) }, [])
  const setLotValue = (key: string, value: string) => setLot((current) => ({ ...current, [key]: value }))
  const saveBatch = async (event: FormEvent) => { event.preventDefault(); try { await putQueue(`/batches/${batch.id}`, { batchCode: editCode, experimentDate: editDate }, 'application/json', 'PATCH'); setEditing(false); setMessage('Batch updated'); load() } catch (e) { setMessage((e as Error).message) } }
  const duplicate = async () => { try { const result = await putQueue(`/batches/${batch.id}/duplicate`, { experimentDate: today(), copyInjectionLots: false }); setMessage(result.queued ? 'Duplicate queued for sync' : 'Batch duplicated') } catch (e) { setMessage((e as Error).message) } }
  const createLot = async (event: FormEvent) => { event.preventDefault(); const positions = lot.wellPositions.split(/[ ,\n]+/).map((value) => value.trim()).filter(Boolean); try { const payload = { ...lot, activatedAt: dateTimeLocalToRFC3339(lot.activatedAt), enuStartAt: lot.enuStartAt ? dateTimeLocalToRFC3339(lot.enuStartAt) : null, enuFinishAt: lot.enuFinishAt ? dateTimeLocalToRFC3339(lot.enuFinishAt) : null, enuPowerPct: lot.enuPowerPct ? Number(lot.enuPowerPct) : null, enuPulseUs: lot.enuPulseUs ? Number(lot.enuPulseUs) : null, enuLed: lot.enuLed ? Number(lot.enuLed) : null, nEggs: lot.nEggs ? Number(lot.nEggs) : null, nActivated: Number(lot.nActivated), wellPositions: positions }; const result = await putQueue(`/batches/${batch.id}/injection-lots`, payload); if (result.queued) setMessage('Lot saved offline; it will sync automatically'); else { setMessage('Lot created'); load() } } catch (e) { setMessage((e as Error).message) } }
  const addEmbryos = async (lotId: string) => { try { const result = await putQueue(`/injection-lots/${lotId}/embryos`, { count }); if (result.queued) setMessage('Embryos queued for sync'); else { setMessage('Embryos added'); load() } } catch (e) { setMessage((e as Error).message) } }
  const updateWell = async (embryo: ApiItem, wellPosition: string | null) => { try { await putQueue(`/embryos/${embryo.id}`, { wellPosition: wellPosition || null }, 'application/json', 'PATCH'); load() } catch (e) { setMessage((e as Error).message) } }
  const preview = Array.from({ length: Math.min(Math.max(Number(lot.nActivated) || 0, 0), 96) }, (_, index) => `${editCode || String(batch.batchCode)}_${lot.lotNo}_${index + 1}`)
  return <section><button className="back" onClick={onBack}>← Batches</button><div className="page-heading"><div><p className="eyebrow">EXPERIMENT</p><h1>{String(batch.batchCode)}</h1><p className="muted">{String(batch.experimentDate)} · {String(batch.siteId ?? '')}</p></div><div className="button-row"><button className="button button--secondary" onClick={() => setEditing(!editing)}>Edit batch</button><button className="button button--secondary" onClick={() => void duplicate()}>Duplicate</button></div></div>{message && <ErrorMessage message={message} />}{editing && <form className="form-card form-card--inline" onSubmit={saveBatch}><label>Batch code<input required value={editCode} onChange={(event) => setEditCode(event.target.value)} /></label><label>Experiment date<input required type="date" value={editDate} onChange={(event) => setEditDate(event.target.value)} /></label><button className="button button--primary">Save changes</button></form>}<form className="form-card" onSubmit={createLot}><h2>Injection lot</h2><div className="form-card--inline"><label>Lot number<input required value={lot.lotNo} onChange={(event) => setLotValue('lotNo', event.target.value)} /></label><label>Donor cell line<select required value={lot.donorCellLineId} onChange={(event) => setLotValue('donorCellLineId', event.target.value)}><option value="">Select donor</option>{donors.map((item) => <option key={String(item.id)} value={String(item.id)}>{String(item.strain ?? item.batchCode ?? item.id)}</option>)}</select></label><label>Activated at<input required type="datetime-local" value={lot.activatedAt} onChange={(event) => setLotValue('activatedAt', event.target.value)} /></label></div><div className="form-card--inline"><label>Eggs<input type="number" min="0" value={lot.nEggs} onChange={(event) => setLotValue('nEggs', event.target.value)} /></label><label>Activated embryos<input required type="number" min="0" max="96" value={lot.nActivated} onChange={(event) => setLotValue('nActivated', event.target.value)} /></label><label>ENU power %<input type="number" min="0" max="100" value={lot.enuPowerPct} onChange={(event) => setLotValue('enuPowerPct', event.target.value)} /></label></div><div className="form-card--inline"><label>ENU pulse µs<input type="number" min="0" value={lot.enuPulseUs} onChange={(event) => setLotValue('enuPulseUs', event.target.value)} /></label><label>ENU LED<input type="number" min="0" value={lot.enuLed} onChange={(event) => setLotValue('enuLed', event.target.value)} /></label><label>ENU start at<input type="datetime-local" value={lot.enuStartAt} onChange={(event) => setLotValue('enuStartAt', event.target.value)} /></label></div><div className="form-card--inline"><label>ENU finish at<input type="datetime-local" value={lot.enuFinishAt} onChange={(event) => setLotValue('enuFinishAt', event.target.value)} /></label><label>Notes<input value={lot.notes} onChange={(event) => setLotValue('notes', event.target.value)} /></label></div><label>Well positions (comma or newline separated)<textarea rows={2} value={lot.wellPositions} placeholder="A1, A2, A3" onChange={(event) => setLotValue('wellPositions', event.target.value)} /></label><button className="button button--primary" type="submit">Create lot</button></form><article className="form-card"><h2>96-well code preview</h2><p className="muted">{preview.length} code(s); verify positions before saving.</p><div className="well-grid">{preview.map((code, index) => <div className="well" key={code}><strong>{wells[index]}</strong><small>{code}</small></div>)}</div></article>{(detail?.injectionLots ?? []).map((item: ApiItem) => <article className="form-card" key={String(item.id)}><div className="page-heading"><div><h2>Lot {String(item.lotNo)}</h2><p className="muted">{String(item.nActivated ?? 0)} activated · {String(item.activatedAt ?? '')}</p></div><button className="button button--secondary" type="button" onClick={() => addEmbryos(String(item.id))}>Add {count} embryos</button></div><label className="form-card--inline">Additional embryos<input type="number" min="1" max="96" value={count} onChange={(event) => setCount(Number(event.target.value))} /></label><div className="table-wrap"><table><thead><tr><th>Well</th><th>Embryo</th><th>Action</th></tr></thead><tbody>{(embryos[String(item.id)] ?? []).map((embryo) => <tr key={String(embryo.id)}><td><select aria-label={`Well for ${String(embryo.embryoCode)}`} value={String(embryo.wellPosition ?? '')} onChange={(event) => void updateWell(embryo, event.target.value)}><option value="">Unassigned</option>{wells.map((well) => <option key={well}>{well}</option>)}</select></td><td>{String(embryo.embryoCode)}</td><td><button className="inline-action inline-action--danger" type="button" onClick={() => void updateWell(embryo, null)}>Clear well</button></td></tr>)}</tbody></table></div></article>)}</section>
}
